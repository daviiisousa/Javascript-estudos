const frutas = ['maça', 'melancia', 'banana', 'kiwi', 'manga']

for(i = 0; i < frutas.length; i++){// a variavel i começa no indice 0 (q seria a maça) e depois ele percorre o array enquanto ele for menor que o tamanho do array e enquanto ele for menor q o array ele vai se somando
    console.log(frutas[i]);
}